# Anthropic API key — optional.
# Without it, the orchestrator runs in mock mode and streams a deterministic
# investigation derived from the actual skill computations on the scenario data.
ANTHROPIC_API_KEY=
